version: '3'

tasks:
  default:
    cmds:
      - task: build
      - task: lint
      - task: test
  build:
    cmds:
      - go build
  lint:
    cmds:
      - go vet ./...
  test:
    preconditions:
      - test -f ./true
    cmds:
      - ./true